{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [
      "/.well-known/*",
      "/css/*"
    ]
  },
  "routes": [
    {
      "route": "/*",
      "rewrite": "/index.html",
      "exclude": [
        "/images/*.{png,jpg,gif,svg}",
        "/css/*",
        "/js/*"
      ]
    }
  ]
}